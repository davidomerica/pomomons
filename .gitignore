# Claude Code local settings and worktrees — never publish these
.claude/

# Dev-only UI screenshot + build-verification harness — never ships
tools/

# Build output. Generated by `node build.js`; published by the Pages workflow,
# never committed.
_site/

# Build-time dependencies (terser / clean-css / html-minifier-terser).
# package.json and package-lock.json ARE committed so CI can npm ci.
node_modules/

# OS junk
Thumbs.db
.DS_Store
