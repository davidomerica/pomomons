{
  "name": "pomomons-build",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling only. The app itself ships as plain HTML/CSS/JS with no runtime dependencies; everything here is dev-time minification.",
  "scripts": {
    "build": "node build.js",
    "serve": "node --run build && npx --yes serve _site"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.36.0"
  }
}
